# Инструкция по запуску HiggsNode на Windows 11

## ✅ Статус готовности

**HiggsNode полностью готов для Windows 11!** 

Все необходимые компоненты реализованы:
- ✅ Поддержка Windows 11/10
- ✅ Автоматическая настройка IP Forwarding
- ✅ Автоматическая настройка маршрутизации
- ✅ Автоматическая настройка Windows Firewall
- ✅ Поддержка WireGuard для Windows
- ✅ Обработчики graceful shutdown с уведомлением bosonserver
- ✅ Автоматические скрипты установки и запуска

## 📋 Требования

1. **Windows 11** (или Windows 10)
2. **WireGuard для Windows** - [скачать и установить](https://www.wireguard.com/install/)
   - ⚠️ **ВАЖНО:** После установки WireGuard **обязательно перезагрузите компьютер**
3. **Node.js 20.x или выше** - [скачать с nodejs.org](https://nodejs.org/)
4. **Права администратора** - обязательно для настройки сети

## 🚀 Быстрый запуск (3 шага)

### Шаг 1: Установка WireGuard и Node.js

1. Установите **WireGuard для Windows**:
   - Перейдите на https://www.wireguard.com/install/
   - Скачайте и установите WireGuard
   - **Перезагрузите компьютер** после установки

2. Установите **Node.js 20.x или выше**:
   - Перейдите на https://nodejs.org/
   - Скачайте LTS версию (рекомендуется)
   - Установите Node.js

### Шаг 2: Установка HiggsNode

**Вариант A: Через .bat файл (самый простой способ)**

1. Откройте папку `higgsnode` в проводнике Windows
2. Щелкните **правой кнопкой мыши** на файл `install.bat`
3. Выберите **"Запуск от имени администратора"**
4. Дождитесь завершения установки

**Вариант B: Через PowerShell**

1. Откройте **PowerShell от имени администратора**:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (администратор)" или "Терминал (администратор)"
2. Перейдите в папку проекта:
   ```powershell
   cd E:\Higgs.net\higgsnode
   ```
3. Запустите скрипт установки:
   ```powershell
   .\install.ps1
   ```

**Что делает скрипт установки:**
- ✅ Проверяет права администратора
- ✅ Проверяет версию Windows
- ✅ Проверяет наличие Node.js и npm
- ✅ Проверяет наличие WireGuard
- ✅ Проверяет сетевые интерфейсы
- ✅ Устанавливает npm зависимости
- ✅ Собирает проект (TypeScript → JavaScript)
- ✅ Создает .env файл из .env.example
- ✅ Создает необходимые директории (logs, dist)

### Шаг 3: Запуск ноды

**Вариант A: Через .bat файл (рекомендуется)**

1. Щелкните **правой кнопкой мыши** на файл `start-node.bat`
2. Выберите **"Запуск от имени администратора"**
3. Нода запустится и начнет работать

**Вариант B: Через PowerShell**

1. Откройте **PowerShell от имени администратора**
2. Перейдите в папку проекта:
   ```powershell
   cd E:\Higgs.net\higgsnode
   ```
3. Запустите ноду:
   ```powershell
   npm start
   ```

**Вариант C: Установка с автоматическим запуском**

```powershell
# Откройте PowerShell от имени администратора
cd E:\Higgs.net\higgsnode
.\install.ps1 -AutoStart
```

## 🔧 Что происходит при первом запуске

При первом запуске нода автоматически:

1. **Генерирует WireGuard ключи** (если еще не созданы)
2. **Включает IP Forwarding** в реестре Windows
   - ⚠️ **Может потребоваться перезагрузка** после первого запуска
3. **Настраивает маршрутизацию** для WireGuard подсети
4. **Настраивает Windows Firewall** правила для forwarding трафика
5. **Регистрируется на BosonServer** (mail.s0me.uk:3003)
6. **Начинает принимать соединения** от клиентов

## 📝 Настройка конфигурации

Файл конфигурации: `higgsnode/.env`

Текущие настройки (уже настроены):
```env
BOSON_SERVER_URL=http://mail.s0me.uk:3003
```

Если нужно изменить настройки:
1. Откройте файл `.env` в текстовом редакторе
2. Измените нужные параметры
3. Перезапустите ноду

## ✅ Проверка работы

После запуска проверьте:

### 1. Проверка IP Forwarding

```powershell
Get-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" -Name "IPEnableRouter"
```

Должно быть: `IPEnableRouter : 1`

### 2. Проверка маршрутов

```cmd
route print
```

Должен быть маршрут для WireGuard подсети (обычно `10.0.0.0`)

### 3. Проверка Firewall правил

```cmd
netsh advfirewall firewall show rule name=all | findstr HiggsNode
```

Должны быть правила с префиксом `HiggsNode-`

### 4. Проверка WireGuard интерфейса

```cmd
wg show
```

Должен показывать активный интерфейс

### 5. Проверка логов

Логи сохраняются в папке `higgsnode/logs/`:
- `higgsnode.log` - основные логи
- `higgsnode-error.log` - ошибки

## 🛑 Остановка ноды

Для корректной остановки:

1. В окне, где запущена нода, нажмите `Ctrl + C`
2. Нода автоматически:
   - Отправит запрос на bosonserver для удаления из реестра
   - Остановит все сервисы
   - Очистит firewall правила
   - Выполнит graceful shutdown

**Важно:** Не закрывайте окно через крестик - используйте `Ctrl + C` для корректного завершения!

## 🔍 Устранение проблем

### Проблема: "WireGuard не найден"

**Решение:**
1. Установите WireGuard с https://www.wireguard.com/install/
2. **Обязательно перезагрузите компьютер** после установки
3. Запустите скрипт установки снова

### Проблема: "Node.js не найден"

**Решение:**
1. Установите Node.js 20.x или выше с https://nodejs.org/
2. Перезапустите терминал после установки
3. Проверьте: `node --version` (должно быть v20.x.x или выше)

### Проблема: "Требуются права администратора"

**Решение:**
1. Щелкните правой кнопкой мыши на скрипт
2. Выберите "Запуск от имени администратора"
3. Или откройте PowerShell/CMD от имени администратора

### Проблема: IP Forwarding не включается

**Решение:**
1. Убедитесь, что запускаете от имени администратора
2. Перезагрузите компьютер после первого запуска
3. Проверьте вручную:
   ```powershell
   Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" -Name "IPEnableRouter" -Value 1 -Type DWord
   ```
4. Перезагрузите компьютер

### Проблема: Трафик не проходит

**Решение:**
1. Проверьте, что физический интерфейс определен правильно:
   ```cmd
   route print 0.0.0.0
   ```
2. Проверьте firewall правила:
   ```cmd
   netsh advfirewall firewall show rule name=all
   ```
3. Проверьте, что WireGuard интерфейс активен:
   ```cmd
   wg show
   ```

### Проблема: Нода не регистрируется на bosonserver

**Решение:**
1. Проверьте URL bosonserver в `.env` файле:
   ```env
   BOSON_SERVER_URL=http://mail.s0me.uk:3003
   ```
2. Проверьте доступность сервера:
   ```powershell
   Test-NetConnection mail.s0me.uk -Port 3003
   ```
3. Проверьте логи в `logs/higgsnode-error.log`

## 📚 Дополнительные команды

```powershell
# Проверка статуса (если доступно)
npm run diagnose

# Пересборка проекта
npm run build

# Режим разработки (с hot reload)
npm run dev

# Остановка ноды
# Нажмите Ctrl+C в окне, где запущена нода
```

## 📖 Дополнительная документация

- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [WINDOWS_SETUP.md](WINDOWS_SETUP.md) - Подробная документация по настройке Windows
- [README.md](README.md) - Общая документация проекта

## 🎯 Итоговая инструкция (краткая версия)

1. **Установите WireGuard** → перезагрузите компьютер
2. **Установите Node.js 20.x**
3. **Запустите `install.bat` от имени администратора**
4. **Запустите `start-node.bat` от имени администратора**
5. **Готово!** Нода работает и принимает соединения

---

*Документ обновлен: Декабрь 2024*

